"use client";

import { ConnectButton } from "@rainbow-me/rainbowkit";

/**
 * Wallet connection button wrapping RainbowKit's ConnectButton.
 * Displays a connect/ disconnect button with account info.
 */
export function WalletConnect() {
  return <ConnectButton />;
}
